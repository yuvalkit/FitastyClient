<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fitastyclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@drawable/fitasty_app_icon"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:roundIcon="@drawable/fitasty_app_round_icon"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainMenuActivity"/>
        <activity android:name=".activities.AccountActivity"/>
        <activity android:name=".activities.MyDietDiariesActivity"/>
        <activity android:name=".activities.DietDiaryActivity"/>
        <activity android:name=".activities.MealActivity"/>
        <activity android:name=".activities.AddDishActivity"/>
        <activity android:name=".activities.AddIngredientActivity"/>
        <activity android:name=".activities.SearchItemsActivity"/>
        <activity android:name=".activities.SettingsActivity"/>
    </application>

</manifest>